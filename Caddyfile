# This app stays on localhost:9000 so your main Caddy can own public 80/443.
# In the main Caddyfile, proxy training.catfacts.cc to http://host.docker.internal:9000.
:80 {
	encode gzip zstd
	reverse_proxy trainer:8000
}
